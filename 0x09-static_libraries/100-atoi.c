#include "main.h"
/**
 * _atoi - print number in string
 * @s: input string
 * Return: number or 0
 */
int _atoi(char *s)
{
	int i = 0;
	int num = 0;

	while (s[i] != '\0')
	{
		num++;
		i++;
	}

	return (0);
}
