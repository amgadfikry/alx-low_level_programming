#include "main.h"
/**
 * infinite_add - add two number
 * @n1: input num
 * @n2: input num
 * @r: result
 * @size_r: size of result
 * Return: added number
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	return (r);
}
