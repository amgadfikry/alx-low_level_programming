#include "main.h"
/**
 * _strchr - locate character in string
 * @s: string input
 * @c: char input
 * Return: location
 */
char *_strchr(char *s, char c)
{
	while (*s)
	{
		if (*s == c)
			return (x);
		s++;
	}
	if (*s == c)
		return (x);

	return (NULL);
}
